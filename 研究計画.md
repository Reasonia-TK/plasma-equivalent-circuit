# 外部回路を考慮したプラズマ・グローバルモデル研究計画

作成日: 2026-09-01

対象回路ソルバー: `C:\Spice64\bin\ngspice_con.exe`

確認済みバージョン: ngspice-46（KLU Direct Linear Solver 有効）

## 1. 結論と研究の狙い

本研究では、まず **Ar、13.56 MHz、平行平板型の容量結合プラズマ（CCP）** を標準問題とする。外部回路は 50 Ω RF 電源、整合回路、ブロッキングコンデンサ、配線・チャンバーの寄生成分で構成し、電気回路は指定された ngspice、プラズマの粒子・電子エネルギー収支は Python の時間依存 0D グローバルモデルで解く。

最初から実機固有値へ合わせ込まず、次の順番で進める。

1. 解析解を持つ RLC 回路で ngspice 入出力と電力計算を検証する。
2. 既報の Ar-CCP／外部整合回路連成問題を再現する。
3. 時間依存グローバルモデルへ拡張し、外部回路との自己無撞着連成を確立する。
4. 圧力、入力、整合回路、寄生成分がプラズマ状態と電力効率へ与える影響を数値実験する。
5. 実機がある場合は、プラズマ OFF 時の回路同定と、プラズマ ON 時の独立測定により検証する。

主たる新規性の候補は、単なる「固定インピーダンスとしてのプラズマ」ではなく、外部回路が変化させる吸収電力によって電子密度・電子温度・シース特性が変わり、その変化が再び回路インピーダンスと高調波へ戻るフィードバックを定量化する点に置く。

## 2. 前提と適用範囲

### 2.1 初期標準問題

実機条件がまだ指定されていないため、第一段階では Schmidt, Mussenbrock, Trieschmann (2018) の公開条件を再現標準とする。

| 項目 | 標準値 |
|---|---:|
| ガス | Ar |
| RF 周波数 | 13.56 MHz |
| 電源振幅 | 100 V |
| 電源内部抵抗 | 50 Ω |
| ガス圧力 | 0.66 Pa |
| ガス温度 | 300 K |
| 駆動電極面積 | 100 cm² |
| 接地側有効面積 | 300 cm² |
| プラズマバルク長 | 5.7 cm |
| 整合回路固定インダクタ | 1.5 µH |
| 整合回路損失 | 0.5 Ω |
| 配線・チャンバー寄生抵抗 | 0.5 Ω |
| 配線・チャンバー寄生容量 | 200 pF |

論文の再現目標値は、電子温度 4.75 eV、電子密度 `1.25e15 m^-3`、整合容量 1550 pF と 175 pF、入力インピーダンス `50.01 - j0.02 Ω`、プラズマ吸収電力がおよそ 5 W である。ただし、再現差を隠すための任意調整は行わず、反応速度式、シース式、数値刻み、電力の符号規約の差として記録する。

### 2.2 初期段階で扱わないもの

- 空間分布、局所的なシース構造、電子エネルギー分布関数の非局所性
- ストリーマ、アーク、異常放電、強い二次電子放出が支配する領域
- 多周波、磁場、ICP コイル、分布定数線路の詳細
- Ar 以外の複雑な反応系

これらは 0D モデルの妥当性確認後に拡張する。特に、グローバルモデルの結果は体積平均量であり、局所値として解釈しない。

## 3. 研究課題と仮説

| ID | 研究課題 | 検証する仮説 |
|---|---|---|
| RQ1 | 外部整合回路を自己無撞着に含める必要があるか | 固定プラズマ負荷で求めた整合点は、密度・温度の回路フィードバックを含めた整合点から有意にずれる |
| RQ2 | 寄生容量・損失・配線は何を変えるか | 基本波の整合が同じでも、プラズマ吸収効率と高調波電流は異なる |
| RQ3 | 非線形シースはどこまで必要か | 線形シース容量では DC 自己バイアスと高調波、プラズマ直列共振を再現できない |
| RQ4 | 圧力・入力による回路とプラズマの相互作用はどう変わるか | 低圧側ほどバルクの電子慣性と非線形シースの影響が強く、外部回路感度が増す |
| RQ5 | モデルは実測へ外挿できるか | 真空時に同定した寄生成分と限定した校正点だけで、未使用条件の電気量・体積平均プラズマ量の傾向を再現できる |

## 4. 数理モデル

### 4.1 時間依存 0D グローバルモデル

種 `s` の粒子収支を次で表す。

$$
\frac{dn_s}{dt}
= \sum_r \nu_{sr} R_r
- \frac{A_{\mathrm{eff},s}}{V_p}\Gamma_{s,\mathrm{wall}}
+ S_{s,\mathrm{flow}}
$$

反応速度は、二体反応なら `R_r = k_r(T_e)n_a n_b` とする。電子エネルギー密度は、電子温度を eV で表して

$$
U_e = \frac{3}{2}n_e eT_e
$$

とし、

$$
\frac{dU_e}{dt}
= \frac{P_{\mathrm{abs}}}{V_p}
- \sum_r \epsilon_r R_r
- P_{e,\mathrm{wall}}/V_p
- P_{\mathrm{elastic}}/V_p
$$

を解く。荷電粒子は初期段階で `n_e ≈ n_Ar+` の準中性を用い、イオン壁損失は Bohm 流束と有効損失面積で与える。

Ar 化学は段階的に導入する。

| モデル | 種 | 用途 |
|---|---|---|
| GM-0 | Ar, Ar+, e | 収支式、単位、保存則の最小検証 |
| GM-1 | Ar, Ar*, Ar+, e | 励起、準安定状態、段階電離を含む標準研究モデル |
| GM-2 | Ar, Ar(m), Ar(r), Ar(4p), Ar+, e | 実験比較で GM-1 の不足が確認された場合のみ導入 |

電子衝突断面積は LXCat のデータセット名、取得日、版、出典を固定して保存し、Maxwellian EEDF で積分した速度係数を基準とする。EEDF 仮定の影響は別の感度解析とし、化学機構変更と回路効果を同時に混ぜない。

### 4.2 CCP の電気的等価回路

プラズマバルクの一般化 Ohm 則を

$$
\frac{dJ}{dt}=\frac{e^2n_e}{m_e}E-\nu_{\mathrm{eff}}J
$$

とし、均一な長さ `l_B`、面積 `A_E` で積分すると、バルクは直列のプラズマインダクタンスと抵抗に対応する。

$$
L_p=\frac{l_Bm_e}{e^2n_eA_E},\qquad
R_p=\nu_{\mathrm{eff}}L_p
$$

`ν_eff` は電子・中性粒子の運動量移行衝突に加え、採用するモデルでは確率加熱の有効項を含める。二つのシースは、次を並列に持つ非線形素子として表す。

- Bohm 流束に基づくイオン電流源
- Maxwellian 電子電流の指数関数型電流源
- シース電圧に依存する非線形容量

シース容量は単に時々刻々の `C(V)` を線形容量へ代入せず、ngspice の **charge-formulated behavioral capacitor** を使い `Q(V)` と `dQ/dV` の整合を取る。`V_s → 0` 付近の特異性には、物理的に説明可能な最小シース電圧または滑らかな正則化を導入し、その幅を感度解析する。

例えば、採用する行列シースモデルで `C_s(V_s)=\sqrt{K/V_s}`、`K=2en_e\epsilon_0A^2` と置く場合、`V_s>0` では `Q_s(V_s)=2\sqrt{KV_s}` として実装し、`dQ_s/dV_s=C_s` を単体テストする。

### 4.3 外部回路

初期回路は次の要素を含む。

```text
13.56 MHz 電源 ─ 50 Ω内部抵抗 ─ L型整合回路 ─ ブロッキングC ─ 駆動電極
                                    │                         │
                                  損失R                  非線形プラズマ
                                    │                         │
                                  寄生C/R ─────────────── 接地電極
```

外部回路の理想素子だけでなく、ESR、コイル直列抵抗、ケーブル・電極の寄生容量を別素子として持たせる。これにより、電源から見た整合と、実際にプラズマへ入る電力を分けて評価できる。

## 5. ngspice とグローバルモデルの連成方式

### 5.1 定常周期解を対象とする第一方式

第一方式は、堅牢で追跡しやすい固定点反復とする。

1. グローバル状態 `x^(k) = {n_s, T_e}` を与える。
2. `L_p`, `R_p`, シース電流・電荷則を計算して netlist を生成する。
3. ngspice で複数 RF 周期の過渡解析を行う。
4. 周期定常に達した最後の整数周期から `v_pl(t)`, `i_pl(t)` を取得する。
5. `P_abs = <v_pl i_pl>`、平均シース電圧、基本波インピーダンス、高調波を計算する。
6. `P_abs` を入力してグローバルモデルを次の低周波時間刻みまで BDF 系で積分する。
7. 必要なら `x^(k+1) ← (1-α)x^(k) + α x_new` と緩和し、収束まで繰り返す。

RF 周期は約 73.75 ns である。ngspice の最大刻みは最初に `T_RF/200` とし、`T_RF/100`, `/200`, `/400` の刻み半減試験で最終採用値を決める。グローバルモデルは反応時定数に合わせた適応刻みを使い、RF 波形を直接追わず、整数周期平均電力を受け取る。

### 5.2 着火・パルス過渡を対象とする第二方式

定常周期連成が成立してから、ngspice の `stop`、`alter`、`resume` を使う単一プロセス連成の実現性を確認する。回路状態を保持したまま、数 RF 周期ごとにプラズマパラメータを更新する。コンソール制御だけで状態保持とパラメータ変更が安定しない場合は、同じ ngspice エンジンの共有ライブラリ方式を代替候補とするが、別ソルバーへ変更しない。

着火・消灯では `n_e → 0` により `L_p → ∞` となるため、単純な値クリップではなく、暗電流・種電子密度・放電前の幾何容量を持つ OFF 状態モデルと ON 状態モデルを定義する。この段階は第一方式の合格後にのみ着手する。

### 5.3 ngspice の実行とデータ交換

- 実行ファイルは設定で固定し、既定値を `C:\Spice64\bin\ngspice_con.exe` とする。
- `.control` 内で `tran`、`wrdata`、必要な `meas` または `fft` を実行し、CSV 互換の表データとログを条件ごとに保存する。
- `.control` を使う netlist は `-b` と混在させない。純粋な batch netlist を使う場合だけ `-b -o <log> <netlist>` とする。
- 実行コマンド、ngspice バージョン、netlist、標準出力、標準エラー、終了コードを保存する。
- 波形を Python 側でも再積分し、ngspice の `.measure` と一致することをテストする。

## 6. 数値実験計画

### 6.1 検証実験

| ID | 実験 | 比較対象 | 合格基準 |
|---|---|---|---|
| V0 | R, RC, RLC の DC/AC/過渡 | 解析解 | 振幅・位相・平均電力誤差 0.1% 未満 |
| V1 | 固定 `n_e`, `T_e` のバルク回路 | `R_p + jωL_p` | 基本波インピーダンス誤差 0.5% 未満 |
| V2 | 非線形シース単体 | `Q(V)` と数値微分 | 電荷保存誤差 0.1% 未満、非有限値なし |
| V3 | 未連成グローバルモデル | 粒子・エネルギー収支 | 定常残差 0.1% 未満、全密度・温度が正 |
| V4 | 2018 年標準問題 | 公開値 | 主要量を原則 10% 以内、差の原因を記録 |
| V5 | 時間刻み半減 | `/100`, `/200`, `/400` | `P_abs`, `n_e`, `T_e`, THD の変化 1% 未満 |
| V6 | 全系電力収支 | 電源・抵抗・プラズマ | 正規化残差 1% 未満 |

固定点反復の収束条件は、連続 3 回で `n_e`, `T_e`, `P_abs` の相対変化がすべて `1e-3` 未満とする。周期定常は、連続 RF 周期の電圧・電流波形の正規化 L2 差が `1e-4` 未満で判定する。

### 6.2 モデル比較実験

同一条件で次の四モデルを比較し、追加した物理の効果を分離する。

1. 固定線形 RLC プラズマ負荷
2. `R_p(n_e), L_p(n_e)` のみ自己無撞着
3. 線形シース容量を持つ自己無撞着モデル
4. 非線形シースと粒子・エネルギー収支を持つ完全モデル

比較応答は、整合容量、`P_abs`、電子密度、電子温度、DC 自己バイアス、基本波入力インピーダンス、電流 THD、3 次・5 次高調波とする。

### 6.3 運転条件マップ

第一の運転条件マップは、圧力と電源振幅の 20 条件とする。

- 圧力: 0.5, 1, 2, 5, 10 Pa
- 電源振幅: 50, 100, 150, 200 V

各条件で以下を行う。

1. 整合容量 `C_m1`, `C_m2` を粗い二次元格子で探索する。
2. 最良点近傍だけ刻みを細かくして再探索する。
3. `|Γ|` 最小、プラズマ吸収電力最大、素子損失最小の各解を保存する。
4. 三つの目的が一致しない場合は Pareto 前面として提示する。

反射係数は基本波入力インピーダンスから

$$
\Gamma=\frac{Z_{in}-50}{Z_{in}+50}
$$

で求める。非線形負荷なので、基本波の `|Γ|` だけを総合効率とはみなさない。

### 6.4 寄生成分・ロバスト性実験

標準点と低圧・高圧の代表点で、次の因子を 3 水準で変える。

| 因子 | 水準例 |
|---|---|
| 寄生容量 | 50, 200, 500 pF |
| 整合回路直列損失 | 0.1, 0.5, 2 Ω |
| コイル | 1.0, 1.5, 2.0 µH |
| 接地／駆動面積比 | 1, 3, 5 |

全組合せを最初から回さず、直交表または縮約要因計画で主効果を選別し、影響が大きい二因子だけ応答曲面を作る。製造公差として容量・インダクタンス ±5%、損失抵抗 ±20% の Monte Carlo を各代表点で 200 回実施し、整合外れとプラズマ吸収電力の分布を評価する。

### 6.5 出力指標

| 分類 | 指標 |
|---|---|
| 回路 | 各節点電圧、枝電流、`Z_in(f0)`、`Γ`、THD、2～12 次高調波、DC 自己バイアス |
| 電力 | 電源供給、内部 50 Ω損失、整合回路損失、寄生損失、プラズマ吸収、総合電力残差 |
| プラズマ | `n_e`, `T_e`, 励起種密度、電離率、イオン壁流束、平均シース電圧、イオンエネルギーの 0D 指標 |
| 数値 | RF 周期収束、固定点収束回数、刻み依存性、失敗率、CPU 時間 |

効率は少なくとも二つを区別する。

$$
\eta_{load}=P_{load}/P_{available},\qquad
\eta_{plasma}=P_{abs}/P_{available}
$$

整合していても整合器・寄生枝で電力を失えば `η_plasma` は低くなるためである。

## 7. 実機検証計画（装置が利用できる場合）

### 7.1 必要な装置

- Ar、真空チャンバー、圧力計、流量制御、排気系
- 13.56 MHz RF 電源、50 Ω系、方向性結合器、整合器、ブロッキングコンデンサ
- RF 対応の高電圧プローブと電流プローブ、十分な帯域のオシロスコープ
- RF 補償 Langmuir プローブ、または独立した `n_e`, `T_e` 診断
- OES（補助診断。単純な線強度比をそのまま電子温度とはみなさない）
- 任意: VNA/LCR メータ、RFEA、校正用 50 Ωダミーロード

RF 高電圧・真空・ガスを扱うため、接地、シールド、インターロック、プローブ定格、放電停止後の残留電荷処理について装置側の安全手順を優先する。

### 7.2 校正とパラメータ同定

1. プラズマ OFF／真空状態で、電源から電極までの開放・短絡・50 Ω負荷応答を測る。
2. ケーブル、整合器、ブロッキング容量、電極の寄生 `R/L/C` を同定する。
3. ngspice のプラズマなしモデルで同じ振幅・位相・損失を再現する。
4. 電圧・電流プローブの時間ずれをダミーロードで補正する。
5. `P=<vi>` の符号、位相、測定位置を固定する。

寄生成分をプラズマ ON データへ自由に合わせ込まない。プラズマ OFF で同定できない壁損失係数などだけを、事前に指定した少数の校正点で決める。

### 7.3 測定計画

実機の安全運転範囲が不明なため、次は候補値である。実際の定格と安定放電範囲を確認して確定する。

- 圧力: 0.66, 2, 5, 10 Pa
- 前進電力: 5, 10, 25, 50 W
- 整合状態: 最適点、`C_m1` の ±5%、`C_m2` の ±5%、両方の ±10%
- 反復: 各条件 3 回

圧力と電力をブロック因子とし、ブロック内の整合ずれ条件をランダム化する。放電開始直後を除き、前進・反射電力および代表発光強度が 10 秒間で 2% 以内になった後に記録する。各試行で、生波形、測定器設定、校正情報、圧力、流量、前進・反射電力、整合器位置、診断結果を保存する。

### 7.4 実測との比較判定

- 電圧・電流 RMS、位相、入力インピーダンス: 目標 10% 以内
- DC 自己バイアス、プラズマ吸収電力: 目標 15% 以内
- 体積平均に対応づけた `n_e`, `T_e`: 目標 30% 以内
- 圧力・電力・整合ずれに対する増減傾向: 符号と順位の一致

0D モデルと局所プローブ値は直接同一ではないため、誤差だけでなく診断の空間位置と体積平均への換算不確かさを併記する。校正に使用していない条件を最低 30% 残し、そこで最終評価する。

## 8. 実装フェーズと成果物

| フェーズ | 期間目安 | 内容 | 完了条件 |
|---|---:|---|---|
| P0 | 1 週 | 実機条件整理、単位・符号規約、データ仕様 | 入力仕様と測定位置が確定 |
| P1 | 1～2 週 | ngspice RLC 検証と等価回路単体 | V0～V2 合格 |
| P2 | 2 週 | GM-0、GM-1 と反応データ固定 | V3 合格、反応台帳完成 |
| P3 | 2 週 | 定常周期の自己無撞着連成 | 固定点収束、電力残差 1% 未満 |
| P4 | 1～2 週 | 2018 年標準問題再現 | V4～V6 合格または差異説明 |
| P5 | 2 週 | 運転マップ、寄生感度、ロバスト性 | RQ1～RQ4 の図表完成 |
| P6 | 2～4 週 | 実機校正・測定・独立検証 | RQ5 の判定 |
| P7 | 追加 | 着火・パルス過渡、GM-2 | 定常モデル合格後に判断 |

想定するリポジトリ構成は次のとおりとする。

```text
configs/          # 幾何、ガス、回路、数値条件
circuits/         # ngspice netlist テンプレート
src/global_model/ # 粒子・電子エネルギー収支
src/cosim/        # netlist生成、ngspice実行、反復制御
data/reactions/   # 断面積・速度係数と出典台帳
data/raw/         # 生波形、ngspiceログ、実測値（追記専用）
data/processed/   # 集計値
tests/            # 解析解、保存則、回帰テスト
reports/          # 図表、条件表、最終報告
```

各計算ケースは、設定ファイルのハッシュ、Git commit、ngspice バージョン、乱数 seed を持つ。失敗ケースも削除せず、終了コードと収束履歴を保存する。

Python 環境は `pyproject.toml` と `uv.lock` で固定し、依存関係の追加・実行は `uv` を使用する。ODE ソルバー、FFT、表データ処理に必要な依存関係は P1～P2 で最小限に確定する。

## 9. 判定ゲート

次のゲートを順番に通過させる。

- [x] G0: 指定 ngspice 実行ファイルの存在と起動確認
- [x] G1: 解析回路の振幅・位相・電力誤差が基準内
- [x] G2: プラズマ等価回路の電荷・電力収支が基準内
- [x] G3: グローバルモデルの粒子・エネルギー収支が基準内
- [x] G4: 2018 年標準問題の再現、または差異の定量説明
- [x] G5: 刻み半減、正則化幅、反復収束に対して解が安定
- [ ] G6: 未使用の実験条件に対する予測検証

G4 より前に広いパラメータ探索を行わず、G5 より前に着火・ヒステリシスの主張を行わない。有限値が出たことや ngspice が正常終了したことは、物理妥当性の証拠とはしない。

## 10. 主なリスクと代替案

| リスク | 兆候 | 対応 |
|---|---|---|
| 固定点反復が振動・発散 | `n_e` と `P_abs` が交互に増減 | 緩和率低下、Anderson 加速、結合残差の監視 |
| シース式の特異性 | 極小刻み、収束失敗、非有限値 | `Q(V)` の滑らかな正則化と幅の感度試験 |
| 高 Q 回路で周期定常が遅い | 周期間 L2 差が下がらない | 初期条件改善、解析時間延長、刻み精密化 |
| グローバルモデルの EEDF 仮定が不十分 | `T_e`、励起種、高調波の系統差 | EEDF 感度解析、GM-2、必要なら Boltzmann 解との接続 |
| 寄生成分とプラズマ損失の識別不能 | 複数パラメータ組が同じ波形 | プラズマ OFF 校正、複数測定位置、検証点の分離 |
| 0D では局所現象を説明できない | 局所診断と体積平均の乖離 | 適用範囲を明記し、必要時だけ 1D/PIC 比較へ進む |

## 11. 実装前に確定すべき装置情報

実機に合わせるには、次を P0 で収集する。

- 放電方式: CCP、ICP、DC、パルスのどれか
- ガス組成、圧力、流量、ガス温度の範囲
- 電極形状、面積、ギャップ、接地面積、誘電体
- 電源周波数、最大電圧／電力、内部インピーダンス、波形
- 整合回路トポロジーと可変範囲、ブロッキング容量、ケーブル長
- 使用可能な電圧・電流・密度・温度・発光・イオン診断
- 研究の主目的: 整合最適化、高調波、着火、密度制御、電力効率の優先順位

これらが得られれば、標準問題を装置条件へ置き換え、数値実験の水準と実測計画を確定できる。

## 12. 主要参考文献・仕様

1. F. Schmidt, T. Mussenbrock, J. Trieschmann, “Consistent simulation of capacitive radio-frequency discharges and external matching networks,” *Plasma Sources Science and Technology* 27, 105017 (2018), [DOI](https://doi.org/10.1088/1361-6595/aae429), [arXiv full text](https://arxiv.org/abs/1804.05638).
2. F. Schmidt et al., “A generic method for equipping arbitrary rf discharge simulation frameworks with external lumped element circuits,” *Physics of Plasmas* 26, 063503 (2019), [DOI](https://doi.org/10.1063/1.5091965), [arXiv full text](https://arxiv.org/abs/1902.02074).
3. A. Hurlbatt et al., “Concepts, Capabilities, and Limitations of Global Models: A Review,” *Plasma Processes and Polymers* 14, 1600138 (2017), [DOI](https://doi.org/10.1002/ppap.201600138).
4. P. Saikia et al., “A nonlinear global model of single frequency capacitively coupled plasma and its experimental validation,” *AIP Advances* 8, 045113 (2018), [DOI](https://doi.org/10.1063/1.5022654).
5. ngspice project, [Documentation and current manual](https://ngspice.sourceforge.io/docs.html). 特に behavioral source、charge-formulated capacitor、transient analysis、`.measure`、`wrdata`、`alter`、`resume` を参照する。
6. LXCat Plasma Data Exchange Project, [project and database information](https://us.lxcat.net/home/).
